// Importazione delle dipendenze principali
import { createApp } from 'vue'
import './style.css'
import App from './App.vue'

// Creazione dell'applicazione Vue e montaggio sul DOM
// L'elemento con id="app" si trova nel file index.html
createApp(App).mount('#app')
